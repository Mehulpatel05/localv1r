rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated users can upload; nobody can read directly (via proxy)
    match /{allPaths=**} {
      allow read: if false; 
      allow write: if request.auth != null;
    }
  }
}
